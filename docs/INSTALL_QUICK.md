# Быстрая установка

```bash
sudo ./setup.sh
```

Для подписанного комплекта укажите доверенный keyring или введите его путь по запросу мастера:

```bash
sudo ./setup.sh --keyring /root/weather-release-keyring.gpg
```
